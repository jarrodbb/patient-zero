{{!-- Future development for med certs  --}}

<div class='row'>
  <div class='col-auto'>
    <h2>Certificates!</h2>
  </div>
</div>

<div class='col-md-6 project-list'>
  <h3>Medical Certificate Requests:</h3>

  {{#each medicalCertificates as |medCert|}}
    <div class='row mb-2'>
      <div class='col-md-8'>
        <h4>{{medCert.reason}}</h4>
      </div>
      <div class='col-md-4'>
        <button
          class='btn btn-sm btn-danger'
          data-id='{{project.id}}'
        >DELETE</button>
      </div>
    </div>
  {{/each}}
</div>

<script src='./js/profile.js'></script>