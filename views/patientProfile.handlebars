<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{patient.name}}!</h2>
    
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6 med-cert">
    <h3>Request a Medical Certificate:</h3>

    <form class="form new-project-form">
      <div class="form-group">
        <label for="doctor-name">Prefered Doctor:</label>
        <select class='form-input' id='doctor-name'>
        <option value='1'>Dr. Dan Javascript</option>
        <option value='2'>Dr. Error Prone</option>
        <option value='3'>Dr. Merge Conflict</option>
      </select>
       
      </div>
      <div class="form-group">
        <label for="project-desc">description:</label>
        <textarea class="form-input" id="medical-reason" name="med-cert"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Request</button>
      </div>
    </form>
  </div>

<div class="col-md-6">
<div class="text-center patient-info">
  <h2>{{name}}</h2>
  <p>Diabetes {{diabetes}}</p>
  <p>heart disease {{heart_disease}}</p>
  <p>High blood pressure {{high_blood_pressure}}</p>
  <p>Kidney or liver disease {{kidney_or_liver_disease}}</p>
  <p>meds {{medication_list}}</p>
  <p> Allergies {{allergies}}</p>

  {{#if medical_certificates.length}}
  <div class="col-md-6 project-list">
    <h3>Med certs:</h3>

    {{#each medical_certificates as |cert|}}
  <div class="delete-button">
    <div class="row mb-2">
      <div class="col-md-8">
        <h4>{{cert.reason}}</h4>
        <h4>Approved? {{cert.approved}}</h4>
        <div class='col-md-4'>
            <button
              class='btn btn-sm btn-danger'
              data-id='{{cert.certificate_id}}'
            >DELETE</button>
          </div>
      </div>
      </div>
    {{/each}}
  </div>
  {{/if}}
<div class="update-button">
    <button class="btn btn-primary" data-id="{{patient_id}}">update profile</button>
  </p>
</div>
<p>
</div>
</div>


<script src="./js/profile.js"></script>


 {{!-- <form class="form new-project-form">
    <div class="form-group">
      <label for="request-reason">Diabetes:</label>
      <select class="form-input" id="request-reason" name="request-reason">
        <option value="yes">Yes</option>
        <option value="no">No</option>
      </select>
    </div>
    <div class="form-group">
      <label for="request-reason">Allergies:</label>
      <select class="form-input" id="request-reason" name="request-reason">
        <option value="nut">Nuts</option>
        <option value="pollen">Pollen</option>
        <option value="none">None</option>
        <option value="other">Other</option>
      </select>
    </div>
    <div class="form-group">
      <label for="request-reason">Heart disease:</label>
      <select class="form-input" id="request-reason" name="request-reason">
        <option value="yes">Yes</option>
        <option value="no">No</option>
      </select>
    </div>
    <div class="form-group">
      <label for="request-reason">High blood pressure:</label>
      <select class="form-input" id="request-reason" name="request-reason">
        <option value="yes">Yes</option>
        <option value="no">No</option>
      </select>
    </div>
    <div class="form-group">
      <label for="request-reason">Kidney or liver disease:</label>
      <select class="form-input" id="request-reason" name="request-reason">
        <option value="yes">Yes</option>
        <option value="no">No</option>
      </select>
    </div>
  </form> --}}